const body = document.querySelector("body");
const firstBreakPoint = 469;
const secondBreakPoint = 599;
body.style.setProperty('--first-break-point', firstBreakPoint);
body.style.setProperty('--first-break-point', secondBreakPoint);